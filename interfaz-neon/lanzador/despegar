#!/bin/bash

QML_PATH="$HOME/ELCAFESIN/interfaz-neon/quantum"
QML_FILE="$QML_PATH/portal.qml"

echo "🚀 Iniciando EL CAFÉ SIN LÍMITES..."
echo "📂 Ruta: $QML_PATH"

export QML2_IMPORT_PATH="$QML_PATH"

# Usar qmlscene (Qt5)
if command -v qmlscene &> /dev/null; then
    echo "✅ Arrancando con Qt5..."
    qmlscene "$QML_FILE"
else
    echo "❌ ERROR: qmlscene no encontrado"
    echo "Instala: sudo apt install qtdeclarative5-dev-tools"
    exit 1
fi
